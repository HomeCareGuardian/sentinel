"""Sentinel shared library (hub transport, host resolution helpers).

Packaged so ``from lib.hub_transport import ...`` works after an editable
install (``pip install -e .``) without a ``sys.path`` shim. See
``[tool.setuptools.packages.find]`` in pyproject.toml.
"""
